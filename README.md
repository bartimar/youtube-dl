# youtube-dl examples

```
youtube-dl --ignore-errors --username user@example.com --password password --continue --extract-audio --audio-format mp3 -o "%(title)s.%(ext)s" 'https://www.youtube.com/playlist?list=blablabla'
```
